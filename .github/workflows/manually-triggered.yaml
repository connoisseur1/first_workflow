name: Manually Triggered
on:
  workflow_dispatch:
    inputs:
      string:
        description: "A string input"
        required: true
        type: string
        default: "Default value"

jobs:
  log-info:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo $GITHUB_SHA
          echo $GITHUB_REF
